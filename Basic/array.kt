/*
package com.gyroh.algorithm

fun main(){


    var arr1 = arrayOf(1,123,"밴댕이",false,5)
    //특정 값을 집어 넣어 배열을 생성
    //특정 타입을 지정하지 않은 상태로 배열을 생성한다.


    제네릭
    var nums = arrayOf<Int>(1,2,3)
    var names = arrayOf<String>("가","나","다")

    제공되는 함수
    var array = intArrayOf(1,2,3)
    var array = charArrayOf('a','b','c')



     // 크기만 설정하여 배열 생성
    예시 ->var array = 자료형Array(갯수, {값})
    var array = CharArray(10)
    var array = IntArray(10)
    var array = booleanArrayOf(10)
    var arr2 = Array(10,{0})

    var num = array[0]
    var num = array.get(0)

    array[1] = 1 //배열명[인덱스] = 넣을값
    array.set(0,1) // 배열명.set(인덱스, 넣을값)

    var arr = Array<Array<Int>>(9){Array<Int>(9){0}}

    val arr = Array(9){IntArray(9)}
    var arr = arrayOf(arrayOf(1,2,3), arrayOf(1,2,3))



    for (i in arr.indices){
        for(j in 0 until arr[i].size){
            if(조건)
            {
                // ~~~
            }
        }
    }

}
 */